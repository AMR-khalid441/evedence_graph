APP_NAME=EVEDENCE_GRAPH
APP_VERSION=0.1.0
OPENAI_API_KEY=
FILE_ALLOWED_TYPES=["pdf","txt","docx"]

FILE_MAX_SIZE=10
FILE_DEFAULT_CHUNK_SIZE=800
# LLM store (optional; defaults used if omitted)
OPENAI_API_URL=
INPUT_DEFAULT_MAX_CHARACTERS=1000
GENERATION_DEFAULT_MAX_TOKENS=1000
GENERATION_DEFAULT_TEMPERATURE=0.1
COHERE_API_KEY=


# Vector DB store
VECTOR_DB_PATH=qdrant_data
VECTOR_DB_DISTANCE_METHOD=cosine
QDRANT_KEY=
QDRANT_CLUSTER_URL=
# Ingest (chunk → embed → vector DB)

# Ingest (chunk → embed → vector DB)
LLM_PROVIDER=OPENAI
VECTOR_DB_PROVIDER=QDRANT
EMBEDDING_MODEL_ID=text-embedding-3-large
EMBEDDING_SIZE=3072
GENERATION_MODEL_ID=gpt-4o
MIN_SCORE_THRESHOLD=0.4